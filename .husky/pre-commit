#!/usr/bin/env sh

npx lint-staged || exit 1

if git grep --cached -n -I -i -E '(?<![A-Za-z0-9])pse(?![A-Za-z0-9])' -- .; then
  echo ""
  echo "Commit blocked: a staged file contains a URL with 'pse'."
  echo "Please remove the potentially internal link and stage the file again."
  exit 1
fi